<div id="meeting">
  <%= render @meeting %>
</div>

<div>
  <ul>
    <li><%= link_to 'Edit', edit_user_meeting_path(@user, @meeting) %></li>
    <li><%= link_to 'View All', user_meetings_path(@user) %></li>
    <li><%= link_to 'Delete', [@user, @meeting], method: :delete, data: {confirm: "Are you sure?"} %></li>
  </ul>
</div>